<Project>
  <Import Project="$(VSToolsPath)\VSSDK\Microsoft.VsSDK.targets" />
  <!-- dotnet build has no Visual Studio deployment directory; package the VSIX but never deploy it. -->
  <PropertyGroup>
    <DeployExtension>false</DeployExtension>
  </PropertyGroup>
</Project>
